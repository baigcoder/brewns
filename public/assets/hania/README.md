Put the ambassador photo here as `hania.jpg` (`.jpeg`, `.png` and `.webp` work too) (portrait, about 4:5, at least
1000 px wide). The polaroid in the #hania section picks it up automatically and
shows the "HA" initials until it exists. Only use a photo you have the rights to.
